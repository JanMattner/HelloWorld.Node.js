{
  "name": "HelloWorld.Node.js",
  "version": "0.1.0",
  "description": "HelloWorld.Node.js",
  "main": "lib/app.js",
  "author": {
    "name": "Jan.Mattner",
    "email": ""
  },
  "devDependencies": {
    "mocha": "^2.2.5"
  },
  "scripts": {
    "test": "mocha",
    "test:server": "mocha --reporter xunit > xunit.xml",
    "preparePackage": "mkdir build && cp lib build -r && cp package.json build && cp README.md build"
  }
}
